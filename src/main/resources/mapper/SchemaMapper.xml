<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.coding_exercise.db.mapper.SchemaMapper">
    
    <resultMap id="SchemaResultMap" type="com.project.coding_exercise.db.model.Schema">
        <id property="id" column="id"/>
        <result property="applicationId" column="application_id"/>
        <result property="serviceId" column="service_id"/>
        <result property="version" column="version"/>
        <result property="filePath" column="file_path"/>
        <result property="uploadedAt" column="uploaded_at"/>
    </resultMap>
    
    <insert id="insert" parameterType="com.project.coding_exercise.db.model.Schema" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO schemas (application_id, service_id, version, file_path) 
        VALUES (#{applicationId}, #{serviceId}, #{version}, #{filePath})
    </insert>
    
    <select id="findLatestByApplicationAndService" resultMap="SchemaResultMap">
        SELECT * FROM schemas 
        WHERE application_id = #{applicationId} 
        AND (service_id = #{serviceId} OR (service_id IS NULL AND #{serviceId} IS NULL))
        ORDER BY version DESC 
        LIMIT 1
    </select>
    
    <select id="findByApplicationServiceAndVersion" resultMap="SchemaResultMap">
        SELECT * FROM schemas 
        WHERE application_id = #{applicationId} 
        AND (service_id = #{serviceId} OR (service_id IS NULL AND #{serviceId} IS NULL))
        AND version = #{version}
    </select>
    
    <select id="findMaxVersionByApplicationAndService" resultType="java.lang.Integer">
        SELECT COALESCE(MAX(version), 0) FROM schemas 
        WHERE application_id = #{applicationId} 
        AND (service_id = #{serviceId} OR (service_id IS NULL AND #{serviceId} IS NULL))
    </select>
    
    <select id="findAllByApplicationAndService" resultMap="SchemaResultMap">
        SELECT * FROM schemas 
        WHERE application_id = #{applicationId} 
        AND (service_id = #{serviceId} OR (service_id IS NULL AND #{serviceId} IS NULL))
        ORDER BY version DESC
    </select>
    
</mapper>
